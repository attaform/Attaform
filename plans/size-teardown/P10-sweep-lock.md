# P10: sweep and lock (STUB, detail at boundary)

Delivers ~-480 B gz eager + tarball ~-80 kB packed + wizard when-used ~-2.9 kB.
Scope: registry fusion (plugin + ssr into registry, one makeTracker, wizard surface
lazy-installed, plain pendingHydration Map); guard()/emitAll shared helper for the
listener-isolation pattern; DEVTOOLS_WINDOW_KEY off the runtime barrel; delete dead
modules (assertions.ts, extract-schema-fields.ts, zod-shape dead exports, orphaned
persistence constants); d.ts two-tier comment policy + audience split (Tier-A cap is
Oswald's call, sign-off 12); wizard internal program at zero eager delta: liveRecord
proxy factory via the shared callableTree, form-less affordance steps (sign-off 10),
import-gated gate spine, guard-inside-helper DCE fix, activeForm handleSubmit
allocation fix, next/tryNext/back/goTo on one guarded navigation core, URL-sync
consolidation; build-transform consolidation (5 -> 1 with substring prefilter +
shared safeExpr) and the one attaform/plugin bundler entry (keep per-bundler
subpaths as thin aliases per judge; collapse internals).
Final acts: full re-baseline (ratchet + size-limit + tarball + attribution map),
SIZE-TEARDOWN.md gets a closing "landed" appendix, status ledger completed.
